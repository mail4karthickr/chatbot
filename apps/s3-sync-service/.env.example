# Postgres connection. docker-compose brings up a matching instance on host port 5433
# (Docker port 5432 is a common conflict, so we map to 5433 on the host).
# Format: postgresql+psycopg://user:password@host:port/dbname
DATABASE_URL=postgresql+psycopg://sync:sync@localhost:5433/sync
